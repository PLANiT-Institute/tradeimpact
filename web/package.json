{
  "name": "ti-web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prebuild": "node scripts/prepare.mjs",
    "predev": "node scripts/prepare.mjs",
    "dev": "next dev",
    "dev:local": "node scripts/prepare.mjs && next dev",
    "package:deploy": "node scripts/prepare.mjs",
    "deploy:vercel": "node scripts/prepare.mjs && vercel",
    "pretest": "node scripts/prepare.mjs",
    "test": "tsx --test tests/*.test.ts tests/*.test.tsx",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.3.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19.2.3",
    "tsx": "^4.23.1",
    "typescript": "^5"
  }
}
