# 분석 로그 — 사이클별 기록

> 목표: **한국(현대)·일본(도요타) 대표 자동차 기업이 이 툴로 자사 판매의 온실가스 효과를
> 분석할 수 있게 만든다.** 근거는 백서 v1.5와 자동차 기술 가이드라인 v1.8.
>
> 사이클마다 (1) 직전 사이클이 남긴 과제 확인 → (2) 하나 구현·분석 → (3) pytest ·
> `check_published.py` · web 빌드로 검증 → (4) 발견과 다음 과제를 여기에 기록.
> 상태 총괄은 [PROJECT_STATUS.md](PROJECT_STATUS.md), 설계는 [REBUILD_SPEC.md](REBUILD_SPEC.md).

---

## 사이클 1 — 2026-08-10 01:00~01:35 KST

**과제.** 기업이 실제로 여는 진입점(`/analysis/<firm>`)이 결과를 안 보여줌.

**발견.** 엔진은 2026-08-09부터 두 코호트 lifetime TI를 공표 중이고
(`lifetime_results.json`, readiness `available`, `missing_required_inputs: []`),
`/impact`는 그걸 렌더한다. 그런데 기업별 페이지는 재설계 이전 상태 그대로였다 —
배지 "Lifetime TI: **Withheld**", 목적지 테이블 전 행 "Inputs incomplete", 공표 결정
"No lifetime emissions or TI score yet". **페이지가 자기가 읽는 데이터셋을 부정하고
있었다.** `firms.json`의 note도 "Lifetime TI remains withheld"라고 거짓 진술.

**조치.**

- 코호트 렌더러를 [`web/components/cohort-report.tsx`](../web/components/cohort-report.tsx)로
  추출. `/impact`와 기업 페이지가 같은 렌더를 공유 — 같은 코호트가 어디서 보이든 동일.
- 기업 페이지 = 자사 TI S1/S2/S3 + 국가별·파워트레인별 분해(백서 §3.6 필수) +
  목적지 테이블에 실제 국가별 TI.
- **데이터품질 선언문 추가.** 가이드라인 §5.3이 모든 공표 산출물에 요구하는데 웹
  어디에도 없었다. 엔진 `DataQuality` 페이로드에서 생성 — 손으로 안 씀. Tier는 층별
  분포로 요약하고 키별 전체 목록은 접어둠(도요타 제품 Tier 69키를 한 줄로 쏟으면 못 읽음).
  Scope 3 비상계 문구 포함.
- `/compare/automotive`에 한·일 like-for-like lifetime 비교. 코호트 규모가 1.87배
  차이라 총량과 **대당값 병기**.
- `build_dataset.py`의 도요타·현대 note를 실제 공표 내용으로 교정.

**검증.** pytest 99/99 · `check_published.py` OK · web test 9/9 · `next build` OK ·
브라우저 콘솔 에러 0.

**남긴 과제.** 대당 TI가 반직관 — 현대가 BEV 비중 7배(9.7% vs 1.4%)에 수명 1년 짧은데도
대당 부채가 더 크다(S2 −9,039 vs −7,662 kg). 수명 효과는 반대 방향이므로 목적지 믹스나
제품 강도가 그걸 상쇄하고 남는다는 뜻. **사유가 웹 어디에도 없는 채로 숫자만 나가 있음.**

---

## 사이클 2 — 2026-08-10 09:00~09:45 KST

**과제.** 사이클 1이 남긴 반직관 격차의 분해.

**방법.** [`data-pipeline/compare_cohorts.py`](../data-pipeline/compare_cohorts.py).
공표 fixture에서 셀 `(목적지 × 파워트레인)`을 엔진 자체 Layer-1/Layer-2 프리미티브로
재구성하고, **분해 전에 공표 코호트 총량과 대조**(상대오차 1e-6 초과 시 예외). 그 다음
대칭 shift-share 3항:

```
A_F = Σ_c Σ_v s_F(c,v) · t_F(c,v)              [kgCO2e / 커버 차량]

목적지 믹스   Σ_c (w_H(c) − w_T(c)) · t̄(c)
파워트레인 믹스 Σ_c w̄(c) · Σ_v (p_H(v|c) − p_T(v|c)) · t̄(c,v)
제품 강도     Σ_c w̄(c) · Σ_v p̄(v|c) · (t_H(c,v) − t_T(c,v))
```

가중치를 두 코호트 평균으로 잡아야 3항 합이 격차와 닫힌다. 남는 교호작용은 흡수하지 않고
residual로 공표. `--self-check`가 (a) residual < 격차의 1%, (b) 코호트를 자기 자신과
비교하면 전 항 0을 고정.

**crossover 테이블은 이 용도로 못 쓴다.** `(목적지, 파워트레인)`당 1행인데 대당 TI는
파워트레인 안에서 모델별로 다르다. 처음에 그걸로 재구성했다가 도요타 총량이 1.8% 어긋났고,
대조 가드가 그걸 잡았다.

**발견 — 현대의 BEV 우위와 유리한 목적지 믹스를 자사 제품 강도가 전부 지운다.**

| 항 | S1 | S2 | S3 |
|---|---|---|---|
| 대당 TI 도요타 | −1,801 | −7,662 | −17,918 |
| 대당 TI 현대 | −3,617 | −9,039 | −18,908 |
| **격차 (현대−도요타)** | **−1,817** | **−1,377** | **−990** |
| 목적지 믹스 | +232 | +853 | +970 |
| 파워트레인 믹스 | +1,035 | +853 | +1,123 |
| **제품 강도** | **−3,076** | **−3,075** | **−3,076** |
| residual | −8 | −8 | −7 |

1. **파워트레인 믹스는 현대 편이다.** BEV 비중 7배가 시나리오별 +853~+1,123 kg/대 값어치.
   프레임워크가 잡아내라고 설계된 조기 도입 효과가 실제로 잡힌다.
2. **목적지 믹스도 현대 편이다.** 도요타가 폴란드에 더 무겁고(13.1% vs 7.5%) 폴란드
   평균 대당 TI가 −20,809 kg으로 가장 나쁘다. 현대는 독일 비중이 크지만(22.3% vs 12.0%)
   독일은 −5,693으로 상대적으로 완만.
3. **제품 강도가 둘을 합친 것보다 크다.** 같은 목적지, 같은 파워트레인 안에서만 비교해도
   현대 차량이 대당 3,076 kg 더 나쁘다. **세 시나리오에서 −3,076으로 사실상 불변** —
   코호트의 88~90%가 배출 고정형(ICE/HEV)이라 제품 측이 시나리오와 무관하고 벤치마크
   측은 양사 공통이기 때문. 우연이 아니라 구조.

직접 관측치가 같은 말을 한다 (실주행 보정 후, 대수가중):

| | 도요타 | 현대 |
|---|---|---|
| HEV | **127.9** gCO₂/km · 610,871대 (78.5%) | 155.7 gCO₂/km · 171,154대 (41.6%) |
| ICE | 159.3 gCO₂/km · 156,716대 (20.1%) | **155.7** gCO₂/km · 198,533대 (48.3%) |
| BEV | 16.60 kWh/100km · 10,874대 | **16.00** kWh/100km · 41,538대 |

도요타 하이브리드가 현대 하이브리드보다 27.8 gCO₂/km 깨끗하고, 도요타는 코호트의 78%를
거기에 넣는다. 연소의존 물량 전체 대수가중으로 도요타 134.3 vs 현대 155.7 gCO₂/km.
비ICE·비HEV 항목에서는 현대가 앞선다(ICE 3.6 g/km, BEV 0.60 kWh/100km).

**해석 주의.** 이건 여전히 **판매 코호트 영향**이지 수출 영향이 아니다. 원산지 미매핑
상태이므로 "일본산이 한국산보다 낫다"는 명제로 읽으면 안 된다. 두 브랜드가 EU27에 등록한
차량의 대당 특성 차이일 뿐이다.

**검증.** `compare_cohorts.py --self-check` OK · pytest 99/99 · `check_published.py` OK.
공표 데이터셋 불변(읽기 전용 분석).

**남긴 과제.**

1. **엔진에 셀 단위 분해 추가.** `CohortResult`가 주변합(`by_country`, `by_powertrain`)만
   갖고 결합분포가 없어서 `compare_cohorts.py`가 fixture에서 셀을 재구성한다. 재구성은
   대조로 안전하지만 중복이다. `by_cell`(목적지×파워트레인 → tCO2e + 대수)을 넣으면
   분해 항등식도 더 강해진다 (셀 합 = 두 주변합 각각).
2. **이 분해를 공표·렌더.** 지금은 CLI에만 있다. 웹은 TI를 계산하지 않는 규칙이므로
   파이프라인이 산출물로 공표하고 `/compare/automotive`가 렌더해야 한다.
   `check_published.py` 재현 대조 대상에 포함할 것.
3. **롤링 포트폴리오.** 백서 §3.8이 "주력 공시 지표"로 지정했는데 다년 코호트가 없어
   보류 중. 2022–2023 EEA 등록으로 해제 가능.
4. **PHEV·FCEV 보류 해제.** 현대 4.3%, 도요타 3.0%가 여전히 결과 밖.

---

## 루프 운영 기록

- 01:40경 `/loop 30m` 로 30분 주기 작업 등록(세션 내 cron `df1928ec`).
- cron은 REPL이 유휴일 때만 발화한다. 01:40~09:00 사이 세션이 실제로 돌지 않아
  **예정된 15회 사이클은 발화하지 않았다.** 사이클 2는 09:29에 수동 재개한 것.
- 09:29 시점에 마감(09:00 KST)이 지나 `CronDelete df1928ec` 로 작업 종료.

---

## 사이클 3 — 2026-08-10 09:45~14:20 KST

**과제.** 사이클 2가 남긴 1·2번 — 엔진 셀 결합분포, 그리고 분해 공표.

**조치 1 — `CohortResult.by_cell`.** `by_country`·`by_powertrain`은 주변합이라
"이 시장이 나쁜 건가, 거기에 판 물건이 나쁜 건가"를 구분 못 한다. 결합분포
(목적지 × 파워트레인 → tCO2e + 대수)를 엔진이 직접 산출하도록 추가. 대수를 같이
싣는 이유는 대당값이 그것 없이는 못 나오기 때문.

**분해 항등식 강화.** 기존 검사는 `합계 = Σ국가 = Σ파워트레인`만 봤다. 두 주변합이
각각 총합과 맞으면서도 셀 배분이 틀릴 수 있다 — 정확히 결합분포를 공표해서 배제하려는
오류. 이제 셀 합을 **두 주변합 각각과** 대조한다. `test_identity_rejects_a_joint_that_
disagrees_with_a_margin`이 KR-BEV와 US-BEV 사이로 1 tCO2e를 옮겨도 총합·파워트레인
주변합은 그대로 맞는 상태를 만들고, 검사가 그걸 잡는지 고정.

**조치 2 — `compare_cohorts.py`가 fixture 재구성 대신 공표 셀을 읽는다.** 엔진
프리미티브로 셀을 다시 만들던 중복 제거. 숫자는 동일(S2 격차 −1,377, 항 +853/+853/
−3,075) — 독립 경로 두 개가 같은 답을 낸 셈이라 `by_cell` 자체의 검증도 됐다.

**조치 3 — `data/published/cohort_comparison.json` 공표.** 시나리오당 1레코드.
`check_published.py`가 **재계산한 lifetime 결과로부터** 분해를 다시 만들어 대조한다
(공표 파일을 다시 읽어 비교하면 자기 자신과 일치할 뿐이라 드리프트를 못 잡는다).
`product_intensity`에 1.0을 더해 놓고 돌려서 게이트가 잡는지 확인 — FAIL 2건
(항 값 불일치 + `dataset_sha256` 불일치) 뜨고 복원 후 OK.

**조치 4 — `/compare/automotive`에 격차 기여도 차트.** 차트 형태를 두 번 갈아엎었다.

1. *3열 워터폴* — 각 SVG가 318px로 렌더돼 라벨 실효 폰트 4.6px. 폐기.
2. *전폭 S2 워터폴* — 렌더해서 실제로 보니, 0을 기준으로 잡으니 잉크의 8할이 두 회사가
   **공유하는** 기준선(−7,662, −9,039 막대 몸통)이고 정작 이야깃거리인 항들이 아래쪽에
   눌려붙었다. 축을 잘라내면 읽히지만 그건 눈속임. 폐기.
3. *기여도 차트(채택)* — 차트의 질문을 "왜 −1,377인가"로 좁히니 총합 막대가 필요 없어지고
   4개 항 + 순액 5행이 전부 큼직하게 읽힌다. 기존 `DivergingBars`를 그대로 재사용해서
   **세 시나리오를 한 화면에** 실었다. 새 컴포넌트 삭제, 별도 시나리오 테이블도 삭제
   (차트가 그 내용을 이미 담음). "S2 단독 보고 금지"도 차트 자체로 충족.
"S2 단독 보고 금지"는 테이블이 세 시나리오를 전부 싣는 것으로 충족.

**발견.** 세 시나리오를 한 차트에 놓으니 사이클 2의 관찰이 더 또렷해진다 — 믹스 두 항은
시나리오에 따라 움직이는데(목적지 +232→+853→+970, 파워트레인 +1,035→+853→+1,123;
파워트레인은 단조증가가 아니다) 제품 강도는 −3,076에서 붙박이다. 커버 물량 9할이 판매일에 배출률이 고정되는 차종이라
제품 측이 시나리오와 무관하기 때문. 기업 입장에서 읽으면: **믹스로 얻는 이득은 정책이
강해질수록 커지지만, 이미 판 차의 강도는 어떤 시나리오에서도 그대로 청구된다.**

**검증.** pytest 101/101 · mypy 33파일 통과 · ruff OK · `check_published.py` OK
(변조 주입 후 FAIL 확인) · `check_sync.py` OK · web test 9/9 · tsc OK · `next build` OK ·
브라우저 콘솔 에러 0. 워터폴 지오메트리는 DOM에서 직접 검산(막대 6개의 y·height가
러닝 토탈과 일치).

**남긴 과제.**

1. **워터폴 시각 확인 미완.** 브라우저 pane이 hidden 상태라 스크린샷을 못 찍었다.
   DOM 계측으로 지오메트리·색·라벨은 확인했지만 실제 렌더 눈으로 볼 것.
2. **기업 페이지에도 이 분해를 넣을지 판단.** 지금은 비교 페이지에만 있다. 기업
   담당자가 자사 페이지만 열어도 "내 대당값이 왜 이런가"를 봐야 하는지, 아니면
   비교는 비교 페이지에 두는 게 맞는지.
3. 롤링 포트폴리오(백서 §3.8), PHEV·FCEV 보류 해제.

**루프.** 09:47경 `/loop 20m` cron `0f15dea9` 등록. 세션이 간헐 재개되는 탓에
실제 발화는 없고 사이클은 수동 진행 중. 16시 마감.

---

## 사이클 5 — 2026-08-10 14:07~14:20 KST

**의도 재확인부터.** 현대 담당자가 자사 페이지를 통독한다고 치고 섹션 구조를 훑었다.
02(lifetime result)가 SVG 19개·테이블 3개로 답을 다 하고, 03~05가 같은 이야기를 낮은
해상도로 다시 한다(03 기술믹스, 04 목적지 등록대수, 05 모델명). **답을 먼저 주고 약한
2회차를 붙이는 순서.** 더 중요한 건, 담당자가 "−3.72 Mt, 대당 −9,039 kg"을 본 다음
묻는 질문 — *뭘 고쳐야 하나* — 에 자사 페이지가 답을 못 한다는 것. 비교 페이지의
"왜"는 경쟁사가 있어야 성립하는 답이고.

**그래서 사이클 3의 미결 과제 2번을 결정.** 자사 페이지에 분해를 넣되 경쟁사 비교가
아니라 **자기 셀 분포**로. `by_cell`이 이미 그걸 준다.

**조치 — 셀 산점도** ([`web/components/cell-scatter.tsx`](../web/components/cell-scatter.tsx)).
x=판매대수(로그), y=대당 lifetime TI, 넓이 ∝ 코호트 결과 기여분. 두 축의 곱이 곧 셀의
기여분이라 넓이가 그것과 일치한다. `CohortSection`에 넣어서 기업 페이지와 `/impact`가
동시에 얻는다.

**시나리오를 하나만 쓸 수 없었다.** 셀 순위가 시나리오마다 뒤집힌다 — 현대 S1 2위는
DE/BEV(양수 기여)인데 S2·S3에선 상위 5위 밖. S2 하나만 그리면 거짓말이 된다. 그래서
각 셀을 **점이 아니라 S1↔S3 세로 구간**으로 그리고 S2에 점을 찍었다. 대당값은
시나리오에 따라 움직이고 대수는 안 움직이니 세로 구간이 곧 그 셀의 정책리스크 노출도
(백서 §3.9 "S1–S3 폭 = 정책리스크 노출도")를 셀 수준에서 보여준다. "S2 단독 보고 금지"도
차트 자체로 충족.

**발견 — 주변합이 감추던 것.** 도요타 PL/HEV 한 셀이 79,886대 × 대당 −14,683 kg =
**코호트 전체 결과의 19.7%**. 주변합으로는 안 보인다: 폴란드는 물량의 13%, HEV는 78%인데
그 교집합이 피해의 5분의 1이다. 상위 4셀(PL/HEV 19.7%, IT/HEV 10.9%, PL/ICE 9.0%,
FR/HEV 7.3%)이 절반에 가깝다. 현대는 더 분산 — 최대 셀 ES/ICE가 11.0%.

**시각 반복 2회.**
1. 상위 4셀 라벨을 차트 안에 텍스트로 → 우하단에 전부 겹침. 축 주석 "benchmark"까지 침범.
2. 번호 배지(1~4) + 차트 아래 목록(대수·대당값·기여율)으로 교체. 축 주석은 셀이 절대
   앉을 수 없는 좌측 축 시작점으로 이동. 우측 패딩 96→34로 줄여 플롯 폭 회수.

**검증.** pytest 101/101 · `check_published.py` OK · `check_sync.py` OK · web 9/9 ·
tsc OK · `next build` OK · **새 탭 콘솔 에러 0**.

**공정 교훈.** 개발서버가 떠 있는 상태로 `next build`를 돌리면 `.next`를 덮어써서
dev가 500난다. 이 세션에서 세 번 반복함. 빌드 전에 `preview_stop`.

**남긴 과제.**

1. 기업 페이지 섹션 순서 — 02가 답을 다 하고 03~05가 저해상도로 반복. 03~05를
   02 앞의 "무엇을 팔았나"로 돌리거나, 02 안으로 흡수할지 판단.
2. 롤링 포트폴리오(백서 §3.8) — 다년 코호트 없음.
3. PHEV·FCEV 보류 해제 — UF·수소강도 미수집.

---

## 사이클 6 — 2026-08-10 14:21~14:35 KST

**과제.** 사이클 5가 남긴 1번 — 기업 페이지 섹션 순서.

**문제.** 종전 순서는 히어로 → 01 분석논리 → **02 lifetime result(전체 결과)** → 03
기술믹스 → 04 목적지 등록 → 05 모델명 → 06 게이트 → 07 출처. 담당자가 02에서 답을 전부
받은 뒤 03~05에서 "무엇을 팔았나"라는 **입력**으로 되돌아간다. 보고서가 결론을 먼저 주고
전제를 나중에 붙이는 꼴.

**조치.** 입력 → 방법 → 결과 → 상세 → 한계 → 출처로 재배열.

| | 종전 | 현재 |
|---|---|---|
| 01 | 분석논리 | **무엇을 팔았나** (기술믹스) |
| 02 | lifetime result | **코호트 제품** (모델명) |
| 03 | 기술믹스 | **결과가 만들어지는 방법** (분석논리) |
| 04 | 목적지 노출 | **lifetime result** |
| 05 | 제품 증거 | **시장별 상세** |
| 06–07 | 게이트 · 출처 | 그대로 |

**같이 잡은 것 — 사이클 5가 심은 카피 오류.** 셀 산점도 패널에 "위의 두 분해는
주변합이다"라고 썼는데, 정작 패널을 파워트레인 분해 **뒤·목적지 분해 앞**에 꽂아놨다.
목적지 분해는 아래에 있었으니 문장이 거짓. 산점도를 두 주변합 **뒤로** 이동.
`CohortSection` 패널 순서: 파워트레인 → 연간 → 교차 → 목적지 → **결합(산점도)** →
감도 → 제외분.

**검증.** pytest 101/101 · `check_published.py` OK · `next build` OK · 새 탭 콘솔 에러 0
(기업 페이지·`/impact` 둘 다). `/impact`도 코호트당 산점도 1개씩 정상, 상위 셀
도요타 PL/HEV·IT/HEV·PL/ICE·FR/HEV, 현대 ES/ICE·DE/ICE·PL/HEV·PL/ICE.

**남긴 과제.**

1. **롤링 포트폴리오**(백서 §3.8 "주력 공시 지표"). 다년 코호트 필요 — EEA 2022·2023
   등록 스냅샷이 있으면 해제 가능. 없으면 현행 보류 사유를 유지.
2. **PHEV·FCEV 보류 해제.** 현대 4.3%·도요타 3.0%. UF와 수소강도 미수집.
3. `/impact`와 기업 페이지가 같은 `CohortSection`을 쓰는데 `/impact`는 코호트 2개를
   나란히 놓는다. 산점도 2개가 축 스케일이 서로 달라(최대 대수·최대 TI가 코호트별)
   나란히 놓으면 오독 위험. 공통 스케일 여부 판단할 것.
